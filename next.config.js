/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cloudinary.zambiarise.com",
      "media.istockphoto.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
